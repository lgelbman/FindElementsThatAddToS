#include <vector>
#include <map>


template <class T>
class FindNumsThatAddToS {

public:

    std::vector<T> find(std::vector<T> vector1, T s) {


        std::map<int, bool> map1;
        for (int i = 0; i < vector1.size; i++) {
            for (int i = 0; i < s; i++) {

            }
        }


        return nullptr;
    }
};